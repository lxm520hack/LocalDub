[project]
name = "voxcpm-torch-server"
version = "0.1.0"
description = "Local VoxCPM TTS Gradio server"
requires-python = ">=3.12"
dependencies = [
    "servers-py",
    "gradio>=6,<7",
    "fastapi",
    "uvicorn[standard]",
    "numpy",
]

[tool.setuptools.packages.find]
include = []
