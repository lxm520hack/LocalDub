[project]
name = "servers-py"
version = "0.1.0"
description = "Shared Python server utilities"
requires-python = ">=3.12"
dependencies = [
    "zeroconf",
]

[tool.setuptools.packages.find]
include = []
