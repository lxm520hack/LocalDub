[project]
name = "demucs-torch-server"
version = "0.1.0"
description = "Local ML Torch server (ASR + Demucs separate)"
requires-python = ">=3.12"
dependencies = [
    "servers-py",
    "fastapi",
    "uvicorn[standard]",
    "numpy",
]

[tool.setuptools.packages.find]
include = []
