# LocalDub Python 运行时依赖
# 安装: pip install -r requirements.txt
# 自动安装（推荐）: bash scripts/setup.sh
#
# GPU 说明：如使用 AMD GPU，PyTorch 需以 CPU-only 版安装（ROCm 不稳定，
# 尤其是 gfx1103/1102 架构）。命令：
#   pip install torch==2.12.0+cpu torchaudio==2.11.0+cpu \
#     --index-url https://download.pytorch.org/whl/cpu

# ── ASR ──
openai-whisper           # 默认 PyTorch Whisper
faster-whisper           # 备用 CTranslate2（精度略低，GPU 兼容性更好）

# ── 音频处理 ──
soundfile                # torchaudio.save/load 的 fallback（替代 torchcodec）
pydub
librosa
numpy

# ── 视频下载 ──
yt-dlp[default]
