# LocalDub Python 运行时依赖
# 安装: pip install -r requirements.txt
# 自动安装（推荐）: bash scripts/setup.sh

# ── ASR ──
openai-whisper           # 默认 PyTorch Whisper
faster-whisper           # 备用 CTranslate2（精度略低，GPU 兼容性更好）

# ── 音频处理 ──
soundfile
pydub
librosa
numpy

# ── 视频下载 ──
yt-dlp[default]
